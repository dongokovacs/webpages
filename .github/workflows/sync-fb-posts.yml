name: Sync Instagram Posts

on:
  schedule:
    - cron: '0 5,11,17,23 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install facebook-scraper
        run: pip install facebook-scraper lxml_html_clean

      - name: Fetch public Facebook posts
        run: python3 sophiegarden/fetch_instagram.py

      - name: Commit if changed
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add sophiegarden/fb-posts.json
          git diff --staged --quiet || (git commit -m "chore: sync Instagram posts [skip ci]" && git push)
